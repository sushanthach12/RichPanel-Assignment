
import { FC } from "react"

interface ConversationProps { }

const Conversation: FC<ConversationProps> = ({ }) => {

    return <h1 className="text-black flex justify-center items-center text-center font-bold text-xl h-full italic">Conversation Appear Here</h1>
}

export default Conversation;